function Education() {
  return (
    <section id="education" style={{
      minHeight: '100vh',
      padding: '6rem 4rem',
      backgroundColor: 'var(--bg-secondary)',
    }}>
    </section>
  )
}

export default Education